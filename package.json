{
  "name": "fixitself",
  "version": "2.0.0",
  "description": "Open Repair Intelligence — Free, unlimited device troubleshooting",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "start:prod": "cd backend && cross-env NODE_ENV=production node server.js",
    "test": "cd backend && npm test",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "clean": "cd backend && npm run clean"
  }
}
